{# @KilikTable/_columnCell.html.twig #}
{# @param table: Kilik\Table #}
{# @param column: Kilik\Column #}
{# @param row: array (from line result) #}

{% if not column.hidden %}
    {% block tableBodyCellOuter %}
        {% set cellLabel = column.label %}
        {% if column.translateDomain is not null %}
            {% set cellLabel = (column.label | trans({}, column.translateDomain)) %}
        {% endif %}
        <td {% if column.displayClass %}class="{{ column.displayClass }}"{% endif %} data-label="{{ cellLabel }}">
            {% block tableBodyCellInner %}
                {% set cellHtml=table.value(column,row) %}
                {% if column.raw %}
                    {{ cellHtml | raw }}
                {% else %}
                    {{ cellHtml }}
                {% endif %}
            {% endblock tableBodyCellInner %}
        </td>
    {% endblock tableBodyCellOuter %}
{% endif %}
