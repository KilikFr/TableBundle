{# KilikTableBundle::_columnName.html.twig #}
{% set label = column.label %}
{% if column.translateDomain is not null %}
    {% set label = (column.label | trans({}, column.translateDomain)) %}
    {% if column.capitalize %}
        {% set label = label | capitalize %}
    {% endif %}
{% endif %}

{% if column.sortable %}
    <th class="columnSortable{% if column.headerClass is not null %} {{ column.headerClass }}{% endif %}" data-column="{{ column.name }}" data-sort-column="{{ column.name }}">
        <a href="#">
            {{ label }}
        </a>
        <span class="glyphicon columnSortableIcon"></span>
    </th>
{% else %}
    <th data-column="{{ column.name }}">
        {{ label }}
    </th>
{% endif %}

